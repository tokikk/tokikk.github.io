{
  "name": "holiday-data",
  "version": "0.1.0",
  "private": true,
  "description": "Fetch Japanese national holidays CSV and publish JSON to docs/ for GitHub Pages",
  "scripts": {
    "fetch-holidays": "node scripts/fetch_holidays.cjs"
  },
  "dependencies": {
    "csv-parse": "^5.5.0",
    "iconv-lite": "^0.6.3"
  }
}

